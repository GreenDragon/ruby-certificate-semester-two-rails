indexer
{
}

searchd
{
  address = 127.0.0.1
  port = 3312
  log = /Users/jhowe/Desktop/RubyCert/rails/rubyholic/log/searchd.log
  query_log = /Users/jhowe/Desktop/RubyCert/rails/rubyholic/log/searchd.query.log
  pid_file = /Users/jhowe/Desktop/RubyCert/rails/rubyholic/log/searchd.development.pid
}

source group_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = rubyholics_development
  sql_sock = /tmp/mysql.sock
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `groups`.`id` * 2 + 0 AS `id` , CAST(`groups`.`name` AS CHAR) AS `name`, CAST(`groups`.`alternate_name` AS CHAR) AS `alternate_name`, CAST(`groups`.`url` AS CHAR) AS `url`, CAST(GROUP_CONCAT(DISTINCT `locations`.`name` SEPARATOR ' ') AS CHAR) AS `location_name`, CAST(GROUP_CONCAT(DISTINCT `locations`.`address` SEPARATOR ' ') AS CHAR) AS `loctiona_address`, IFNULL(`groups`.`name`, '') AS `name_sort`, IFNULL(`groups`.`alternate_name`, '') AS `alternate_name_sort`, IFNULL(`groups`.`url`, '') AS `url_sort`, UNIX_TIMESTAMP(`groups`.`created_at`) AS `created_at`, UNIX_TIMESTAMP(`groups`.`updated_at`) AS `updated_at`, GROUP_CONCAT(DISTINCT `locations`.`id` SEPARATOR ',') AS `location_ids`, `groups`.`id` AS `sphinx_internal_id`, 2885774273 AS `class_crc`, '2885774273' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM groups    LEFT OUTER JOIN `locations` ON locations.group_id = groups.id  WHERE `groups`.`id` >= $start   AND `groups`.`id` <= $end    GROUP BY `groups`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `groups` 
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_str2ordinal = name_sort
  sql_attr_str2ordinal = alternate_name_sort
  sql_attr_str2ordinal = url_sort
  sql_attr_multi = uint location_ids from field
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `groups` WHERE  `id` = (($id - 0) / 2)
}

index group_core
{
  source = group_core_0
  path = /Users/jhowe/Desktop/RubyCert/rails/rubyholic/db/sphinx/development/group_core
  morphology = stem_en
  charset_type = utf-8
}

index group
{
  type = distributed
  local = group_core
}

source location_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = rubyholics_development
  sql_sock = /tmp/mysql.sock
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT `locations`.`id` * 2 + 1 AS `id` , CAST(`locations`.`name` AS CHAR) AS `name`, CAST(`locations`.`address` AS CHAR) AS `address`, CAST(`locations`.`latitude` AS CHAR) AS `latitude`, CAST(`locations`.`longitude` AS CHAR) AS `longitude`, CAST(`groups`.`name` AS CHAR) AS `group_name`, CAST(`groups`.`alternate_name` AS CHAR) AS `group_alternate_name`, CAST(`groups`.`url` AS CHAR) AS `group_url`, IFNULL(`locations`.`name`, '') AS `name_sort`, IFNULL(`locations`.`address`, '') AS `address_sort`, IFNULL(`locations`.`latitude`, '') AS `latitude_sort`, IFNULL(`locations`.`longitude`, '') AS `longitude_sort`, UNIX_TIMESTAMP(`locations`.`created_at`) AS `created_at`, UNIX_TIMESTAMP(`locations`.`updated_at`) AS `updated_at`, `locations`.`group_id` AS `group_id`, `locations`.`id` AS `sphinx_internal_id`, 2817059741 AS `class_crc`, '2817059741' AS `subclass_crcs`, 0 AS `sphinx_deleted` FROM locations    LEFT OUTER JOIN `groups` ON `groups`.id = `locations`.group_id  WHERE `locations`.`id` >= $start   AND `locations`.`id` <= $end    GROUP BY `locations`.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`id`), 1), IFNULL(MAX(`id`), 1) FROM `locations` 
  sql_attr_uint = group_id
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = class_crc
  sql_attr_uint = sphinx_deleted
  sql_attr_timestamp = created_at
  sql_attr_timestamp = updated_at
  sql_attr_str2ordinal = name_sort
  sql_attr_str2ordinal = address_sort
  sql_attr_str2ordinal = latitude_sort
  sql_attr_str2ordinal = longitude_sort
  sql_attr_multi = uint subclass_crcs from field
  sql_query_info = SELECT * FROM `locations` WHERE  `id` = (($id - 1) / 2)
}

index location_core
{
  source = location_core_0
  path = /Users/jhowe/Desktop/RubyCert/rails/rubyholic/db/sphinx/development/location_core
  morphology = stem_en
  charset_type = utf-8
}

index location
{
  type = distributed
  local = location_core
}
